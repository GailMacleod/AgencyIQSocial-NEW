# NPM Configuration for Deployment
# Fixes cache corruption issues

cache=/tmp/.npm
update-notifier=false
fund=false
audit=false
progress=true
loglevel=verbose

# Force fresh downloads
prefer-offline=false
offline=false

# Production optimizations
production=false
include=dev

# Registry settings
registry=https://registry.npmjs.org/